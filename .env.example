# https://supabase.com/dashboard — Project Settings → API (or Connect dialog)
NEXT_PUBLIC_SUPABASE_URL=
# Use Publishable key (new) or anon key (legacy) — never commit the service_role key
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
